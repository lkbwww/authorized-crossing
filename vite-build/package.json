{
  "name": "authorized-crossing-vite",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:unit": "node --test ../shared/tests/*.test.js",
    "test:e2e": "E2E_PORT=4273 PLAYWRIGHT_BROWSERS_PATH=.playwright-browsers playwright test",
    "test:e2e:headed": "E2E_PORT=4273 PLAYWRIGHT_BROWSERS_PATH=.playwright-browsers playwright test --headed",
    "test:e2e:ui": "E2E_PORT=4273 PLAYWRIGHT_BROWSERS_PATH=.playwright-browsers playwright test --ui",
    "test:e2e:install": "PLAYWRIGHT_BROWSERS_PATH=.playwright-browsers playwright install chromium"
  },
  "dependencies": {
    "phaser": "3.90.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "vite": "^5.4.11"
  }
}
